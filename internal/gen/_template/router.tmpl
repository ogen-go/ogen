{{- /*gotype: github.com/ogen-go/ogen/internal/gen.TemplateConfig*/ -}}

{{ define "router" }}
{{ $pkg := $.Package }}
{{ template "header" $ }}

func Register(r chi.Router, s Server, opts ...Option) {
{{- range $op := $.Operations }}
    r.MethodFunc("{{ $op.Spec.HTTPMethod }}", "{{ $op.Spec.Path }}", New{{ $op.Name }}Handler(s, opts...))
{{- end }}
}
{{ end }}
