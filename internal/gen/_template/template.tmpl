{{- /*gotype: github.com/ernado/ogen/internal/gen.config*/ -}}

{{ define "main" }}
{{ $pkg := $.Package }}
{{ template "header" $ }}

{{ range $s:= $.Components }}
// {{ $s.Name }} is {{ $s.Path }}.
//
// {{ $s.Description }}
type {{ $s.Name }} struct {
	{{ range $f := $s.Fields }}
		{{- $f.Name }} {{ $f.Type }} `json:"{{ $f.TagName }}"`
	{{ end }}
}
{{ end }}

{{ end }}