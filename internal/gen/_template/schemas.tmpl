{{- /*gotype: github.com/ogen-go/ogen/internal/gen.config*/ -}}

{{ define "schemas" }}
{{ $pkg := $.Package }}
{{ template "header" $ }}

{{ range $s:= $.Schemas }}
// {{ $s.Name }} is {{ $s.Path }}.
//
// {{ $s.Description }}
type {{ $s.Name }} struct {
	{{ range $f := $s.Fields }}
		{{- $f.Name }} {{ $f.Type }} `json:"{{ $f.TagName }}"`
	{{ end }}
}
{{ end }}

{{ end }}