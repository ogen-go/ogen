{{ define "json" }}
{{- /*gotype: github.com/ogen-go/ogen/gen.TemplateConfig*/ -}}

{{ $pkg := $.Package }}
{{ template "header" $ }}

{{- range $t := $.Types }}
{{ if $t.IsStruct }}
// Encode implements json.Marshaler.
func (s {{ $t.Name }}) Encode(e *jx.Encoder) {
	e.ObjStart()
	{{- range $f := $t.JSON.Fields }}
		{{ template "enc_json" field_elem $f -}}
	{{- end }}
	e.ObjEnd()
}

// Decode decodes {{ $t.Name }} from json.
func (s *{{ $t.Name }}) Decode(d *jx.Decoder) error {
	if s == nil {
		return errors.New(`invalid: unable to decode {{ $t.Name }} to nil`)
	}
	return d.ObjBytes(func(d *jx.Decoder, k []byte) error {
		switch string(k) {
	{{- range $f := $t.JSON.Fields }}
	case "{{ $f.Tag.JSON }}":
		{{- template "dec_json" field_elem $f }}
	{{- end }}
		default:
			return d.Skip()
		}
		return nil
	})
}
{{- else if $t.IsGeneric }}
{{ $g := $t.GenericOf }}
{{ $v := $t.GenericVariant }}
// Encode encodes {{ $g.Go }} as json.
func (o {{ $t.Name }}) Encode(e *jx.Encoder{{ if $g.Format }}, format func(*jx.Encoder, {{ $g.Go }}){{ end }}) {
{{- if $v.Nullable }}
	if o.Null {
		e.Null()
		return
	}
{{- end }}
{{- if $g.Format }}
	format(e, o.Value)
{{- else if $g.JSON.Format }}
	json.Encode{{ $g.JSON.Format }}(e, o.Value)
{{- else if $g.JSON.Fn }}
	e.{{ $g.JSON.Fn }}({{ $g.Primitive }}(o.Value))
{{- else if $g.IsStruct }}
	o.Value.Encode(e)
{{- end }}
}

// Decode decodes {{ $g.Go }} from json.
func (o *{{ $t.Name }}) Decode(d *jx.Decoder{{ if $g.Format }}, format func(*jx.Decoder) ({{ $g.Go }}, error){{ end }}) error {
	if o == nil {
		return errors.New(`invalid: unable to decode {{ $t.Name }} to nil`)
	}
	switch d.Next() {
	case jx.{{ $g.JSON.Type }}:
	{{- if $v.Optional }}
		o.Set = true
	{{- end }}
	{{- if $v.Nullable }}
		o.Null = false
	{{- end }}
	{{- if $g.Format }}
		v, err := format(d)
		if err != nil {
			return err
		}
		o.Value = v
	{{- else if $g.JSON.Format }}
		v, err := json.Decode{{ $g.JSON.Format }}(d)
		if err != nil {
			return err
		}
		o.Value = v
	{{- else if $g.JSON.Fn }}
		v, err := d.{{ $g.JSON.Fn }}()
		if err != nil {
			return err
		}
		o.Value = {{ $g.Go }}(v)
	{{- else if $g.IsStruct }}
		if err := o.Value.Decode(d); err != nil {
			return err
		}
	{{- end }}
		return nil
	{{- if $v.Nullable }}
	case jx.Null:
		if err := d.Null(); err != nil {
			return err
		}
		var v {{ $g.Go }}
		o.Value = v
		{{- if $v.Optional }}
		o.Set = true
		{{- end }}
		o.Null = true
		return nil
	{{- end }}
	default:
		return errors.Errorf(`unexpected type %q while reading {{ $t.Name }}`, d.Next())
	}
}
{{- else if $t.IsEnum }}
// Encode encodes {{ $t.Name }} as json.
func (s {{ $t.Name }}) Encode(e *jx.Encoder) {
	e.{{ $t.JSON.Fn }}({{ $t.Primitive }}(s))
}

// Decode decodes {{ $t.Name }} from json.
func (s *{{ $t.Name }}) Decode(d *jx.Decoder) error {
	if s == nil {
		return errors.New(`invalid: unable to decode {{ $t.Name }} to nil`)
	}
	v, err := d.{{ $t.JSON.Fn }}()
	if err != nil {
		return err
	}
	*s = {{ $t.Name }}(v)
	return nil
}
{{- else if $t.IsSum }}
// Encode encodes {{ $t.Name }} as json.
func (s {{ $t.Name }}) Encode(e *jx.Encoder) {
	switch s.Type {
	{{- range $s := $t.SumOf }}
	case {{ $s.Name }}{{ $t.Name }}:
		{{- template "enc_json" elem $s (printf "s.%s" $s.Name) -}}
	{{- end }}
	}
}

{{ $d := $t.JSON.Sum -}}
// Decode decodes {{ $t.Name }} from json.
func (s *{{ $t.Name }}) Decode(d *jx.Decoder) error {
	if s == nil {
		return errors.New(`invalid: unable to decode {{ $t.Name }} to nil`)
	}
	// Sum type {{ $d }}.
	{{- if or ($d.Primitive) ($d.TypeDiscriminator) }}
	switch t := d.Next(); t {
	{{- range $s := $t.SumOf }}
	case jx.{{ $s.JSON.Type }}:
		{{- template "dec_json" elem $s (printf "s.%s" $s.Name) }}
		s.Type = {{ $s.Name }}{{ $t.Name }}
	{{- end }}
	default:
		return errors.Errorf("unexpected json type %q", t)
	}
	{{- else if or ($d.Fields) ($d.Discriminator) }}
	if d.Next() != jx.Object {
		return errors.Errorf("unexpected json type %q", d.Next())
	}
	var found bool
	if err := d.Capture(func(d *jx.Decoder) error {
		return d.ObjBytes(func(d *jx.Decoder, key []byte) error {
			if found {
				return d.Skip()
			}
			switch string(key) {
			{{- if $d.Discriminator -}}
			case "{{ $t.SumSpec.Discriminator }}":
				typ, err := d.Str()
				if err != nil {
					return err
				}
				switch typ {
				{{- range $m:= $t.SumSpec.Mapping }}
				case "{{ $m.Key }}":
					s.Type = {{ $m.Type }}{{ $t.Name }}
					found = true
				{{- end }}
				default:
					return errors.Errorf("unknown type %s", typ)
				}
				return nil
			{{- else }}
			{{- range $s := $t.SumOf }}
			{{- range $f := $s.SumSpec.Unique }}
			case "{{ $f.Tag.JSON }}":
				found = true
				s.Type = {{ $s.Name }}{{ $t.Name }}
			{{- end }}
			{{- end }}
			{{- end }}
			}
			return d.Skip()
		})
	}); err != nil {
		return errors.Wrap(err, "capture")
	}
	if !found {
		return errors.New("unable to detect sum type variant")
	}
	switch s.Type {
	{{- range $s := $t.SumOf }}
	case {{ $s.Name }}{{ $t.Name }}:
		{{- template "dec_json" elem $s (printf "s.%s" $s.Name) }}
	{{- end}}
	default:
		return errors.Errorf("inferred invalid type: %s", s.Type)
	}
	{{- else if $d.Discriminator  }}

	{{- end }}
	return nil
}
{{- else if $t.IsAlias }}
// Encode encodes {{ $t.Name }} as json.
func (s {{ $t.Name }}) Encode(e *jx.Encoder) {
	unwrapped := {{ $t.AliasTo.Go }}(s)
	{{- template "enc_json" elem $t.AliasTo "unwrapped" -}}
}

// Decode decodes {{ $t.Name }} from json.
func (s *{{ $t.Name }}) Decode(d *jx.Decoder) error {
	if s == nil {
		return errors.New(`invalid: unable to decode {{ $t.Name }} to nil`)
	}
	var unwrapped {{ $t.AliasTo.Go }}
	if err := func() error {
		{{- template "dec_json" elem $t.AliasTo "unwrapped" }}
		return nil
	}(); err != nil {
		return errors.Wrap(err, "alias")
	}
	*s = {{ $t.Go }}(unwrapped)
	return nil
}
{{- end }}

{{- end }}
{{ end }}
