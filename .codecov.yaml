ignore:
  # Ignore generated files.
  - "**/oas_*_gen.go"
  # Ignore commands (examples or internal utilities).
  - "cmd/*/*.go"
  - "**/cmd/*/*.go"
  # Ignore examples
  - "examples/**"

coverage:
  status:
    project: false
    patch: false
